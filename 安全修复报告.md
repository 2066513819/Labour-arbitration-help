# 安全漏洞修复报告

## 概述

根据大赛评委反馈，项目存在三个安全缺陷：**硬编码 API Key**、**固定 SMS 验证码**、**CORS 通配符**。本报告记录修复详情。

| 修复项 | 严重程度 | 状态 |
|--------|----------|------|
| 硬编码 API Key（3 处） | 🔴 高 | ✅ 已修复 |
| 固定 SMS 验证码明文暴露 | 🔴 高 | ✅ 已修复 |
| CORS 通配符 + allow_credentials | 🟡 中 | ✅ 已修复 |

---

## 一、硬编码 API Key

### 问题描述

多处代码将腾讯云元器 Agent ID、API Key 直接写死在 `.py` 源文件中，提交到代码仓库后会造成密钥泄露。

### 涉及文件

#### 1. `backend/app/services/image_agent_service.py`

**修复前：**
```python
app_id = "2043649085851829312"
api_key = "EYlR4WZ5wkyke1hhyf7OKsVACcfuQiwJ"
```

**修复后：**
```python
from app.config import settings

app_id = settings.document_agent_id
api_key = settings.document_agent_api_key
if not app_id or not api_key:
    raise RuntimeError("未配置文书起草智能体 API 密钥 (DOCUMENT_AGENT_ID / DOCUMENT_AGENT_API_KEY)")
```

#### 2. `backend/app/services/document_workflow_service.py`

**修复前：**
```python
WORKFLOW_APP_ID = "2043940782090381120"
WORKFLOW_API_KEY = "Vdti9RYvG6JBzBLEosFe9KKbJ8mya7qs"
```

**修复后：**
```python
from app.config import settings

WORKFLOW_APP_ID = settings.workflow_agent_id
WORKFLOW_API_KEY = settings.workflow_api_key
WORKFLOW_API_BASE = settings.workflow_api_base
```

### 配套修改

#### `backend/app/config.py`

新增配置项，全部从环境变量读取：

```python
# 文书起草智能体 API（图片上传）
document_agent_id: str = ""
document_agent_api_key: str = ""

# 文书起草工作流 API
workflow_agent_id: str = ""
workflow_api_key: str = ""
workflow_api_base: str = "https://yuanqi.tencent.com/openapi/v1"
```

#### `backend/.env`

密钥统一集中在 `.env` 文件中管理：

```env
DOCUMENT_AGENT_ID=2043649085851829312
DOCUMENT_AGENT_API_KEY=EYlR4WZ5wkyke1hhyf7OKsVACcfuQiwJ
WORKFLOW_AGENT_ID=2043940782090381120
WORKFLOW_API_KEY=Vdti9RYvG6JBzBLEosFe9KKbJ8mya7qs
```

#### `.env.example` 模板

已更新为不含真实密钥的模板文件，提交代码时使用此文件：

```env
# 文书起草智能体 API（图片上传）
DOCUMENT_AGENT_ID=
DOCUMENT_AGENT_API_KEY=

# 文书起草工作流 API
WORKFLOW_AGENT_ID=
WORKFLOW_API_KEY=
```

---

## 二、固定 SMS 验证码

### 问题描述

1. 后端 `/send-code` 接口将明文验证码通过 `dev_hint` 字段返回给前端
2. 前端 `Login.vue` 在登录页面上直接显示验证码 `123456`
3. 攻击者无需手机即可直接获取验证码

### 修复措施

#### `backend/app/config.py`

开发环境留空时自动生成随机验证码，生产环境必需为空：

```python
if settings.environment == "development" and not settings.dev_sms_code:
    settings.dev_sms_code = "".join(secrets.choice("0123456789") for _ in range(6))
    print(f"[安全] 开发环境自动生成了随机短信验证码")
```

#### `backend/app/routers/auth.py`

移除 `dev_hint` 返回字段，验证码仅打印到服务端日志：

```python
@router.post("/send-code")
def send_code(body: SendCodeRequest):
    # 开发环境：打印验证码到服务端日志，不暴露到前端
    if settings.environment == "development":
        print(f"[开发] 手机号 {body.phone} 的验证码: {settings.dev_sms_code}")
    return {
        "ok": True,
        "message": "验证码已发送",
        "ui_meta": {"animation": "toast", "duration_ms": 250},
    }
```

#### `frontend/src/views/Login.vue`

移除验证码显示组件和 `devHint` 变量，不再从接口获取并展示验证码。

---

## 三、CORS 通配符

### 问题描述

`allow_origins=["*"]` 配合 `allow_credentials=True` 是浏览器直接禁止的不安全组合。任何网站都可以携带凭证跨域请求本服务。

### 修复措施

#### `backend/app/config.py`

新增 CORS 配置项：

```python
cors_origins: str = "http://localhost:5173"
```

#### `backend/app/main.py`

从环境变量读取允许的来源域名列表：

```python
from app.config import settings

cors_origins = [o.strip() for o in settings.cors_origins.split(",") if o.strip()]
app.add_middleware(
    CORSMiddleware,
    allow_origins=cors_origins,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

生产环境可配置为：

```env
CORS_ORIGINS=https://your-frontend-domain.com
```

---

## 四、额外安全增强

### JWT 密钥自动生成

```python
if not settings.secret_key or settings.secret_key == "change-me-in-production-use-openssl-rand-hex-32":
    if settings.environment == "production":
        raise RuntimeError(
            "生产环境必须设置强随机 SECRET_KEY。请运行: "
            "python -c \"import secrets; print(secrets.token_hex(32))\""
        )
    settings.secret_key = secrets.token_hex(32)
    print("[安全] 开发环境自动生成了随机 SECRET_KEY")
```

- 开发环境：`.env` 中留空则自动生成随机密钥
- 生产环境：必须手动设置强随机值，否则启动报错

---

## 五、修改文件清单

| 文件 | 修改类型 |
|------|----------|
| `backend/app/config.py` | 新增配置项 + 安全启动检查 |
| `backend/app/services/image_agent_service.py` | 移除硬编码密钥，改为读取配置 |
| `backend/app/services/document_workflow_service.py` | 移除硬编码密钥，改为读取配置 |
| `backend/app/routers/auth.py` | 移除 `dev_hint` 明文返回 |
| `backend/app/main.py` | CORS 来源列表改为从配置读取 |
| `frontend/src/views/Login.vue` | 移除验证码明文展示 |
| `backend/.env` | 密钥集中管理 |
| `backend/.env.example` | 更新为不含真实密钥的模板 |

---

## 六、使用说明

1. 将 `.env` 加入 `.gitignore`，确保不会被提交到代码仓库
2. 新建环境时复制 `.env.example` 为 `.env`，填入真实密钥
3. 生产环境务必设置强随机 `SECRET_KEY`，并将 `ENVIRONMENT=production`
4. 生产环境配置真实的短信验证码服务，将 `DEV_SMS_CODE` 留空
5. 生产环境将 `CORS_ORIGINS` 设置为实际前端域名

---

> 修复日期：2026-05-23
