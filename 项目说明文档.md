# 劳动仲裁智能辅助系统 - 项目说明文档

## 一、项目背景与目标

### 1.1 问题现状

劳动争议仲裁是劳动者维权的重要途径，但普通劳动者在仲裁过程中面临诸多困难：

劳动者对劳动法律知识了解有限，不知道自己属于哪种劳动者类型（正式工、劳务派遣、实习生、平台工作者等），难以判断应适用哪些法律条款。准备仲裁材料时，不知道需要哪些证据，证据清单不明确。仲裁申请书等专业文书的撰写存在门槛，许多劳动者不知道如何规范书写。赔偿金额计算涉及工作年限、工资标准、离职原因等多重因素，计算方法复杂。

### 1.2 项目目标

本系统旨在为普通劳动者提供一站式仲裁辅助服务，降低维权门槛，提高维权效率。系统通过智能化手段，帮助劳动者快速确定自身类型、理解仲裁流程、准备证据材料、生成规范文书、估算合理赔偿金额。

---

## 二、系统架构

### 2.1 技术栈

**后端**：Python FastAPI 框架，提供 RESTful API 接口，支持高并发异步处理。

**前端**：Vue 3 + Vite + Element Plus + TailwindCSS，采用组件化开发，响应式设计适配多端设备。

**数据库**：SQLite（开发环境），生产环境可切换为 MySQL，存储用户信息、案件数据、证据记录。

**AI服务**：集成腾讯元器智能体 API，实现自然语言对话和文档智能分析。

**文件存储**：腾讯云 COS 对象存储，存储用户上传的证据图片和文档。

### 2.2 项目结构

```
劳动仲裁辅助系统/
├── backend/
│   ├── app/
│   │   ├── routers/        # API路由（登录、案件、证据、文书、仲裁知识等）
│   │   ├── models/         # SQLAlchemy数据模型
│   │   ├── services/       # 业务逻辑（AI服务、文档工作流等）
│   │   ├── schemas/        # Pydantic数据验证模型
│   │   ├── data/           # 静态数据（场景目录、高频问题等）
│   │   └── deps.py         # 依赖注入（认证、数据库会话）
│   ├── main.py             # 应用入口
│   └── database.py         # 数据库配置
│
├── frontend/
│   ├── src/
│   │   ├── views/          # 页面组件（首页、案件管理、智能对话等）
│   │   ├── components/     # 公共组件（身份选择弹窗等）
│   │   ├── stores/         # Pinia状态管理
│   │   ├── api/            # HTTP请求封装
│   │   └── router/         # Vue Router配置
│   └── package.json
│
└── 项目配置文件
```

---

## 三、核心功能模块

### 3.1 身份类型判定

#### 功能说明

许多劳动者不确定自己属于哪种用工类型，不同类型适用不同的法律条款和仲裁流程。系统提供交互式问答判定，帮助用户明确自身类型。

#### 实现逻辑

判定流程分为多个步骤，每步展示一个判断问题：

```
步骤1: "您是否与用人单位直接签订劳动合同？"
  → 否：继续下一步
  → 是：判断为正式工

步骤2: "您是否由劳务派遣公司派遣到当前单位工作？"
  → 是：判断为劳务派遣员工
  → 否：继续下一步

步骤3: "您是否在校学生，以实习为主业？"
  → 是：判断为在校实习生
  → 否：继续下一步

步骤4: "您是否通过外卖、快递、网约车等平台接单工作？"
  → 是：判断为平台工作者
  → 否：判断为其他类型
```

#### 技术实现

后端存储判定问题的配置数据，前端按步骤展示问题并收集用户答案。用户完成判定后，系统根据答案组合计算最终类型，返回类型标签和判定理由。用户确认后，类型信息存储到用户资料，后续所有功能自动应用该类型配置。

### 3.2 智能对话咨询

#### 功能说明

用户可以通过自然语言描述遇到的问题，AI助手根据用户选择的劳动者类型，返回针对性的法律建议、相关法律条款、建议操作指引。

#### 咨询类型

系统支持多种咨询场景：

| 咨询类型 | 说明 | 返回内容 |
|---------|------|---------|
| 正式工 | 劳动关系认定 | 适用法律、维权途径 |
| 劳务派遣 | 同工同酬、社保 | 派遣法规、证据要求 |
| 实习 | 报酬拖欠、协议违约 | 民法条款、协商建议 |
| 平台工 | 加班费、订单罚款 | 裁判规则、举证要点 |
| 帮我做判断 | 不确定类型 | 引导判定流程 |
| 案例检索 | 查找类似判例 | 案例摘要、结果参考 |
| 法规检索 | 查询具体条款 | 法规原文、适用范围 |

#### 技术实现

用户选择劳动者类型和咨询场景后，输入问题内容。后端将问题、劳动者类型、历史对话（如果有）组装为请求，通过腾讯元器智能体 API 发送。AI 返回结构化回复，包含问题复述、法律依据、解读分析、建议操作。回复内容以卡片形式展示，支持继续追问保持上下文。

### 3.3 案件管理

#### 功能说明

用户可以创建、管理多个仲裁案件，每个案件关联特定的劳动者类型和纠纷场景。案件作为容器，组织相关的证据材料、文书草稿、计算结果。

#### 数据模型

案件记录包含以下核心字段：

- 案件ID：系统生成的唯一标识
- 用户ID：案件所属用户
- 劳动者类型：正式工/劳务派遣/实习生/平台工等
- 案件标题：用户自定义的案件名称
- 纠纷简述：用户描述的纠纷概况
- 进展阶段：创建→证据→文书→完成
- 创建时间、更新时间

#### 操作流程

用户创建案件时填写标题和纠纷简述。创建后可在案件详情页查看进展，补充证据材料，生成文书草稿，保存计算结果。每个案件的操作相互独立，便于管理多个并行的仲裁案件。

### 3.4 证据管理

#### 功能说明

证据是仲裁成败的关键，但普通劳动者往往不知道需要哪些证据。系统根据用户选择的劳动者类型和纠纷场景，自动推送对应的证据清单，指导用户有针对性地准备。

#### 证据类型

系统支持多种证据类型的上传和管理：

| 证据类型 | 说明 | 处理方式 |
|---------|------|---------|
| 劳动合同 | 证明劳动关系存在 | OCR识别提取关键信息 |
| 工资流水 | 证明工资金额 | OCR识别、计算月均工资 |
| 社保记录 | 证明社保缴纳情况 | 图片识别 |
| 考勤记录 | 证明工作时间 | 图片识别 |
| 解除通知 | 证明离职原因 | OCR识别 |
| 聊天截图 | 证明沟通内容 | 图片识别 |
| 其他文档 | 补充证明材料 | PDF/Word解析 |

#### OCR识别

用户上传图片后，系统调用OCR接口识别图片中的文字，提取关键信息。识别结果展示给用户校对，确认后可关联到案件。OCR技术大大降低了手动录入的工作量，同时保证了信息的准确性。

#### 云端存储

上传的证据文件存储到腾讯云COS对象存储，生成唯一访问URL。证据记录关联到具体案件，用户可随时查看、下载。COS提供高可用、高可靠的存储服务，确保证据文件安全持久保存。

#### 一键发送到文书

证据管理页面每条证据支持一键发送到文书起草模块。点击"起草"按钮后，系统自动将证据的URL、类型、名称传递给文书起草页面，无需重复上传，实现证据到文书的流畅衔接。

### 3.5 文书起草

#### 功能说明

仲裁申请书是仲裁的核心文书，格式规范、内容完整是受理的前提。许多劳动者不知道如何撰写，系统通过AI辅助，从证据材料中提取关键信息，自动生成规范的仲裁申请书草稿。

#### 工作流程

文书起草分为三个阶段：

**阶段一：材料上传**

用户可选择上传图片（劳动合同、工资条、通知函等）或文档（PDF、Word）。图片可通过拖拽或点击上传，文档支持多种格式。上传后系统自动处理。

**阶段二：智能分析**

上传材料后，系统调用AI工作流分析内容：

```
输入：证据图片/文档
处理：OCR识别 + 内容理解 + 关键信息提取
输出：结构化的证据摘要
```

AI分析完成后，以对话形式展示提取的信息，用户可补充或纠正。证据材料中的关键信息（公司名称、姓名、入职日期、工资标准、离职原因等）被自动识别并结构化。

**阶段三：文书生成**

用户确认信息后，系统根据劳动者类型、证据内容、仲裁请求生成完整的仲裁申请书。文书包含以下部分：

- 申请人信息（姓名、身份证、电话、地址）
- 被申请人信息（公司名称、统一社会信用代码、地址）
- 仲裁请求（经济补偿金、未发工资、加班费等各项金额）
- 事实与理由（基于证据材料生成）
- 证据清单（关联已上传的证据）
- 落款（申请人签名、日期）

#### 技术实现

文书生成调用AI服务，根据预设的模板结构和提取的信息内容，填充生成最终文书。用户可在线编辑修改，确认后保存。最终文书可导出为Word/PDF格式打印使用。

### 3.6 金额估算

#### 功能说明

赔偿金额的计算直接关系到仲裁请求的合理性。金额过高可能被驳回，金额过低则损失权益。系统提供多种场景的金额计算工具，帮助用户估算合理的赔偿范围。

#### 计算场景

**经济补偿金（精确模式）**

输入入职日期、离职日期、前12个月平均月薪、离职原因，系统按以下公式计算：

```
经济补偿金 = 月工资 × 工作年限

工作年限：
- 每满1年计1个月
- 6个月以上不满1年计1个月
- 不满6个月计0.5个月
- 最高不超过12个月

特殊情况：
- 违法解除：2倍经济补偿金
- 协商一致解除：N个月
- 合同到期不续签：N个月
```

**经济补偿金（简易模式）**

直接输入月工资金额和工作年限，快速计算结果。

**延时加班费**

输入加班小时数、小时工资率（通常为日工资÷8），按以下公式计算：

```
工作日加班费 = 小时工资 × 1.5 × 加班小时数
休息日加班费 = 小时工资 × 2 × 加班小时数
法定节假日加班费 = 小时工资 × 3 × 加班小时数
```

**欠发工资**

输入应发工资金额，系统直接显示欠发总额（不含滞纳金部分）。

**罚款争议核对**

输入罚款金额，系统根据法律规定判断是否超过法定上限。《工资支付暂行规定》规定罚款金额不得超过当月工资的20%。

#### 技术实现

金额计算在前端完成，包含完整的计算公式和边界条件处理。计算结果展示详细的计算过程，便于用户理解。最终结果可关联到案件，用于文书中的仲裁请求部分。

### 3.7 流程指引

#### 功能说明

仲裁有固定的流程和时效要求，错过时效将丧失胜诉权。系统根据用户类型展示针对性的仲裁流程指引。

#### 指引内容

**管辖仲裁委**

说明当事人应向哪个仲裁委申请：
- 用人单位注册地的区劳动仲裁委
- 合同履行地的区劳动仲裁委

提供百度地图链接，可搜索附近的仲裁委员会地址和联系方式。

**仲裁步骤**

标准仲裁流程：

```
步骤1：收集证据
  └─ 整理劳动合同、工资记录、社保缴纳记录、解除通知等

步骤2：准备材料
  └─ 仲裁申请书、身份证复印件、企业信息、证据复印件

步骤3：提交申请
  └─ 前往仲裁委提交材料，填写申请书
  └─ 仲裁委5日内决定是否受理

步骤4：参加庭审
  └─ 按时出席，陈述事实，出示证据
  └─ 可申请调解或继续等待裁决

步骤5：领取裁决
  └─ 裁决书送达后15日内可向法院起诉
  └─ 逾期不起诉，裁决生效
```

**材料清单**

根据劳动者类型和纠纷场景，列出需要准备的具体材料：

正式工离职补偿所需材料：
- 身份证原件及复印件
- 劳动合同原件及复印件
- 解除/终止劳动合同证明
- 工资发放记录（银行流水）
- 社保缴费记录
- 考勤记录（如有）
- 相关聊天记录、通知函等

#### 地图集成

点击"地图检索"按钮，在新窗口打开百度地图，自动搜索用户所在区域的劳动仲裁委员会，显示地址、电话等信息。方便用户实地前往提交申请。

---

## 四、数据管理

### 4.1 用户认证

用户通过手机号注册，输入验证码完成登录。系统使用JWT（JSON Web Token）进行身份认证，Token有效期7天。用户信息包含手机号、劳动者类型、注册时间。

### 4.2 数据隔离

每个用户只能访问自己的案件、证据、文书数据。后端API通过依赖注入获取当前用户，确保数据隔离。用户删除账号时，关联数据一并清理。

### 4.3 进展追踪

案件记录包含进展阶段字段，系统追踪记录操作历史：

```
创建案件 → 上传证据 → 生成文书 → 提交仲裁
    ↓          ↓           ↓           ↓
  stage=0    stage=1    stage=2    stage=3
```

用户可在首页查看所有案件及当前进展，便于管理多个仲裁事务。

---

## 五、劳动者类型与场景细分

### 5.1 类型分类体系

系统针对五类劳动者提供差异化服务：

| 类型 | 标识 | 典型场景 |
|-----|------|---------|
| 正式工 | employee | 入离职争议、经济补偿 |
| 劳务派遣 | dispatch | 同工同酬、社保责任 |
| 在校实习 | intern | 报酬拖欠、协议违约 |
| 平台工作 | platform | 外卖骑手、网约车司机 |
| 其他/不确定 | other_uncertain | 需引导判定 |

### 5.2 场景目录

每种劳动者类型对应多个细分纠纷场景：

**正式工场景**

- 违法解除劳动合同
- 协商一致解除补偿
- 未支付经济补偿金
- 未签劳动合同双倍工资
- 未足额支付加班费
- 年休假工资争议
- 工伤待遇争议

**劳务派遣场景**

- 同工同酬争议
- 社保缴纳责任划分
- 派遣合同纠纷
- 离职补偿争议
- 岗位调整纠纷

**在校实习场景**

- 实习报酬拖欠
- 实习协议违约
- 实习期间人身伤害
- 实习证明出具纠纷

**平台工作场景**

- 加班工资/在线时长争议
- 订单罚款/扣款纠纷
- 劳动关系认定争议
- 平台算法规则争议

每个场景包含：场景ID、标题、证据提示、法律依据。用户选择场景后，系统自动推送对应的证据清单和法律条款。

---

## 六、法律知识库

### 6.1 高频问题

系统根据劳动者类型，推送相关的高频仲裁问题及参考答案：

```
正式工高频问题：
Q：被公司口头辞退怎么办？
A：要求公司出具书面解除通知，同时收集工作证、工资记录等证据。可向仲裁委申请确认劳动关系解除违法。

Q：未签劳动合同如何维权？
A：收集事实劳动关系证据（工资、考勤、工作安排），可主张入职第2个月起至第12个月的双倍工资。
```

### 6.2 法律条款库

系统预置常用劳动法律条款，按类型和场景关联：

```
核心法规：
- 《中华人民共和国劳动合同法》
- 《中华人民共和国劳动法》
- 《劳动争议调解仲裁法》
- 《工伤保险条例》
- 《劳务派遣暂行规定》
- 《新就业形态劳动者权益保障办法》
```

用户查询时，系统根据当前劳动者类型和场景，过滤显示相关的法律条款和司法解释。

---

## 七、安全与性能

### 7.1 安全措施

- JWT Token认证，7天有效期
- 密码使用bcrypt加密存储
- 用户数据隔离，后端强制校验所有权
- 短信验证码防刷，限制发送频率
- 文件上传限制大小和类型，防止恶意文件
- SQL注入防护，使用ORM参数化查询
- XSS防护，前端输出转义

### 7.2 性能优化

- FastAPI异步处理，支持高并发
- 数据库连接池复用
- 前端路由懒加载，按需加载页面
- 图片压缩存储，减少带宽占用
- CDN加速静态资源访问

### 7.3 错误处理

- 全局异常捕获，返回统一错误格式
- 前端错误提示，用户友好的错误信息
- 后端日志记录，便于问题排查
- AI服务降级，本地规则兜底

---

## 八、部署说明

### 8.1 环境要求

- Python 3.10+
- Node.js 18+
- npm 9+ 或 pnpm

### 8.2 配置项

环境变量配置文件（.env）：

```env
# 数据库
DATABASE_URL=sqlite:///./labor_arb.db

# JWT密钥
SECRET_KEY=your-secret-key

# 腾讯元器API
YUANQI_API_BASE=https://yuanqi.tencent.com/openapi/v1/agent/chat/completions
YUANQI_API_KEY=your-api-key
YUANQI_AGENT_ID=your-agent-id

# 腾讯云COS
COS_SECRET_ID=your-secret-id
COS_SECRET_KEY=your-secret-key
COS_BUCKET=your-bucket
COS_REGION=ap-nanjing
```

### 8.3 启动方式

**后端启动**

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

**前端启动**

```bash
cd frontend
npm install
npm run dev
```

访问 http://localhost:5173 使用系统。

---

## 九、创新与亮点

### 9.1 智能判定，降低门槛

通过交互式问答判定劳动者类型，解决普通用户"不知道自己是什么类型"的核心痛点。

### 9.2 全流程覆盖

从身份判定→证据准备→文书生成→金额计算→流程指引，提供一站式服务，避免用户在多个平台之间切换。

### 9.3 AI赋能

利用大语言模型理解用户问题、解析证据内容、生成规范文书，降低专业文书的撰写门槛。

### 9.4 类型细分

针对正式工、劳务派遣、实习生、平台工作者分别配置场景库、证据清单、法律条款，提供精准匹配的服务。

### 9.5 证据串联

证据管理支持一键发送到文书起草，实现材料的顺畅流转，减少重复操作。

---

## 十、使用流程示例

### 场景：张先生被公司违法辞退

**第一步：类型判定**

张先生不知道自己属于哪种类型，进入系统后点击"类型判定"，回答几个简单问题后，系统判定为"正式工"。

**第二步：智能咨询**

张先生描述被辞退的经过，AI助手分析后建议："您的情况属于违法解除劳动合同，可主张2N倍经济补偿金。建议收集：劳动合同、工资流水、辞退通知、社保记录。"

**第三步：创建案件**

张先生在"我的案件"中创建新案件，填写标题"公司违法辞退赔偿"。

**第四步：上传证据**

张先生上传劳动合同、12个月工资流水、公司出具的解除通知。系统OCR识别提取关键日期和金额。

**第五步：金额估算**

张先生进入金额计算，输入入职日期、离职日期、月薪25000元，系统计算违法解除赔偿金为50000元（2个月×25000元）。

**第六步：生成文书**

张先生进入文书起草，系统已加载案件中的证据材料。AI分析证据后生成仲裁申请书草稿，包含2N赔偿金请求。张先生在线修改确认后保存。

**第七步：查看流程指引**

张先生查看流程指引，了解到需准备身份证复印件、企业信息等材料，获得仲裁委地址，准备提交申请。

---

*文档版本：1.0*
*更新时间：2026年4月*
