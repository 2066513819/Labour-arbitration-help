# 劳动仲裁辅助系统 - 环境安装与启动手册

## 📋 项目概述

本系统是一个劳动仲裁辅助平台，采用前后端分离架构：

- **后端**：Python FastAPI
- **前端**：Vue 3 + Vite + Element Plus + TailwindCSS
- **数据库**：SQLite（开发环境）/ MySQL（生产环境）
- **AI服务**：腾讯元器智能体 API

---

## 🖥️ 系统要求

- **操作系统**：Windows 10/11、macOS、Linux
- **Python**：3.10 或更高版本
- **Node.js**：18.x 或更高版本
- **npm**：9.x 或更高版本（推荐使用 pnpm）

---

## 📦 后端安装与启动

### 1. 进入后端目录

```bash
cd backend
```

### 2. 创建虚拟环境（推荐）

**Windows PowerShell：**
```powershell
python -m venv venv
.\venv\Scripts\Activate
```
### 3. 安装依赖

```bash
pip install -r requirements.txt
```
### 5. 初始化数据库

```bash
# Windows
python -m app.database init

# 或直接运行（会自动创建数据库）
python main.py
```

### 6. 启动后端服务

```bash
# 开发模式（推荐，支持热重载）
 uvicorn app.main:app --reload
```

后端服务地址：`http://localhost:8000`

### 7. 验证后端启动

访问 API 文档：http://localhost:8000/docs

---

## 🌐 前端安装与启动

### 1. 进入前端目录

```bash
cd frontend
```

### 2. 安装依赖

```bash
npm install
# 或使用 pnpm（更快）
# pnpm install
```

### 3. 启动开发服务器

```bash
npm run dev
```

前端服务地址：`http://localhost:5173`（或 `http://localhost:5174`，取决于端口占用情况）

### 4. 构建生产版本

```bash
npm run build
```

构建产物在 `dist` 目录中。

---

## 🔧 配置说明

### 腾讯元器智能体 API

1. 访问 [腾讯元器平台](https://yuanqi.tencent.com)
2. 创建或选择智能体
3. 获取以下信息：
   - **API 地址**：`https://yuanqi.tencent.com/openapi/v1/agent/chat/completions`
   - **API Key**：在平台获取
   - **Agent ID**：智能体的 ID

### 腾讯云 COS 配置

1. 访问 [腾讯云控制台](https://console.cloud.tencent.com)
2. 开通对象存储服务（COS）
3. 创建存储桶，获取：
   - SecretId
   - SecretKey
   - Bucket 名称
   - 地域（如 ap-nanjing）

---

## 📁 项目目录结构

```
劳动仲裁辅助系统/
├── backend/                    # 后端目录
│   ├── app/
│   │   ├── api/               # API 路由
│   │   ├── models/            # 数据库模型
│   │   ├── services/          # 业务逻辑
│   │   ├── schemas/           # Pydantic 模型
│   │   └── ...
│   ├── main.py               # 应用入口
│   ├── database.py           # 数据库配置
│   ├── requirements.txt      # Python 依赖
│   └── .env                  # 环境变量
│
├── frontend/                   # 前端目录
│   ├── src/
│   │   ├── api/              # API 请求
│   │   ├── assets/           # 静态资源
│   │   ├── components/       # 公共组件
│   │   ├── stores/           # Pinia 状态管理
│   │   ├── views/            # 页面组件
│   │   ├── router/           # 路由配置
│   │   └── App.vue           # 根组件
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
│
└── README.md                  # 项目说明
```

---

## 🚀 快速启动（推荐）

在项目根目录下执行以下命令：

### Windows

```powershell
# 启动后端（在 backend 目录）
cd backend
python -m venv venv
.\venv\Scripts\Activate
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# 新开终端，启动前端（在 frontend 目录）
cd frontend
npm install
npm run dev
```

### macOS / Linux

```bash
# 启动后端
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000 &

# 启动前端
cd frontend
npm install
npm run dev
```

---

## 🔐 登录说明

开发环境短信验证码固定为：**123456**

---

## ❗ 常见问题

### 1. 后端启动报错 "ModuleNotFoundError"

确保已激活虚拟环境并安装依赖：
```bash
pip install -r requirements.txt
```

### 2. 前端无法连接后端

检查后端是否启动在 `http://localhost:8000`，并检查前端的 API 代理配置。

### 3. 依赖安装失败

- **Python 依赖**：使用国内镜像
  ```bash
  pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```

- **Node 依赖**：使用国内镜像
  ```bash
  npm install --registry=https://registry.npmmirror.com
  ```

### 4. 数据库问题

删除 `backend/labor_arb.db` 文件，重新启动后端会自动创建新数据库。

### 5. 元器 API 调用失败

- 检查 API Key 和 Agent ID 是否正确
- 检查网络是否能访问腾讯元器服务
- 查看后端日志中的错误信息

---

## 📞 技术支持

如有问题，请查看：
- 后端 API 文档：http://localhost:8000/docs
- 前端源码注释
- 后端日志输出

---

## 📝 注意事项

1. **生产环境**：务必更换 `SECRET_KEY` 为安全的随机字符串
2. **API Key**：不要将真实的 API Key 提交到代码仓库
3. **数据库**：生产环境建议使用 MySQL
4. **跨域**：开发环境已配置 CORS，生产环境请正确配置

---

*更新时间：2026年4月*
